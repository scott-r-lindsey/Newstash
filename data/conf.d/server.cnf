
[mysqld]

default_authentication_plugin=mysql_native_password

binlog_expire_logs_seconds = 86400

log_bin = /var/lib/mysql-binlogs/binlog

# this buffers log writes for performance
innodb_flush_log_at_trx_commit = 2

